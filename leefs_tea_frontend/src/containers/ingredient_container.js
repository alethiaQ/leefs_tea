import { Box, Card, CardHeader, CardBody, Heading, Stack, StackDivider, Text } from '@chakra-ui/react'
import * as list from '../mock_ingredients.json'
const IngredientContainer = () => {

    return (
        <Box className="">
    
        </Box>
    )
}
export default IngredientContainer;